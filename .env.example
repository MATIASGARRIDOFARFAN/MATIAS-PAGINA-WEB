# SQLite (desarrollo local — funciona sin instalar nada extra)
DATABASE_URL="file:./dev.db"

# SQL Server (producción / campus) — descomenta y ajusta:
# DATABASE_URL="sqlserver://localhost:1433;database=usmp_market;user=sa;password=TuPassword;encrypt=true;trustServerCertificate=true"

# Secreto para firmar sesiones (genera uno aleatorio en producción)
AUTH_SECRET="cambia-este-secreto-por-uno-seguro"
